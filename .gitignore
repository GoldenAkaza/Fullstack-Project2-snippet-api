# Dependencies - installed from package.json, never committed

# Node.js third-party libraries - huge, can be regenerated by 'npm install'
node_modules/

# Environment files - may contain secrets or local configuration
# The main file containing actual secrets (never commit this)
.env

# Any environment files (e.g., .env.development, .env.production)
.env.* 


# Logs and caches - noisy files created at runtime or by development tools
# Any file ending in .log
*.log

# A common directory for log files
logs/ 

# Debug file created by npm (package manager)
npm-debug.log

# Cache file for ESLint (code linter)
.eslintcache 

# Build outputs and coverage - files that can be regenerated from source code
# Common directory for distribution/production ready code
dist/

# Another common directory for built output
build/

# Test coverage reports
coverage/

# OS and editor artefacts - machine specific files that don't belong in Git
# macOS file system metadata
.DS_Store

# Windows file system metadata
Thumbs.db

# Visual Studio Code settings for this workspace
.vscode/

# IntelliJ IDEA IDE settings for this workspace
.idea/